#!/bin/sh

# Test lp filter underlining

$FILTDIR/filter_lp "" "testprn" "" <${TEST_BASENAME}.in1 || echo "error: $?"

$FILTDIR/filter_lp "" "testprn" "" <${TEST_BASENAME}.in2 || echo "error: $?"

exit 0
