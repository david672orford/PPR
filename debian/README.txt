mouse:~ppr/src/debian/README.txt
20 June 2005

This directory is used to package PPR for Debian GNU/Linux.  Make sure you 
have the following non-obvious packages installed.

	libgdbm-dev
	tdb-dev
	devscripts
	gpg				(to sign the package)
	fakeroot
	debhelper

